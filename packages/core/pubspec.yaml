name: core
description: Core domain abstractions and base classes
publish_to: none
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  shared:
    path: ../shared

  # Either pattern
  dartz: ^0.10.1

  # Equality
  equatable: ^2.0.5

  # UUID
  uuid: ^4.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
