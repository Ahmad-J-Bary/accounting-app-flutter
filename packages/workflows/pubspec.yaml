name: workflows
description: Workflow orchestration layer for coordinating features
publish_to: none
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  foundation:
    path: ../foundation
  platform:
    path: ../platform
  
  # Features
  partners:
    path: ../features/partners
  sales:
    path: ../features/sales
  inventory:
    path: ../features/inventory
  accounting:
    path: ../features/accounting

  dartz: ^0.10.1
  equatable: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
