name: foundation
description: Shared utilities and helpers for accounting app
publish_to: none
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  # Validation
  formz: ^0.6.1

  # Either pattern for error handling
  dartz: ^0.10.1

  # UUID generation
  uuid: ^4.3.1

  # Date time
  intl: ^0.20.2

  # Equality
  equatable: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
