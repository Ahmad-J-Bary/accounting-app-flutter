name: production
description: Production feature package
publish_to: none
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  foundation:
    path: ../../core
  foundation:
    path: ../../shared
  platform:
    path: ../../networking
  platform:
    path: ../../storage
  inventory:
    path: ../inventory
  products:
    path: ../products

  dartz: ^0.10.1
  equatable: ^2.0.5
  uuid: ^4.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
