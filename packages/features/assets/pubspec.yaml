name: assets
description: Fixed Assets and Consumables Management Module for ERP System
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Architecture
  dartz: ^0.10.1
  equatable: ^2.0.5
  
  # Foundation (local)
  foundation:
    path: ../../foundation
  
  # State Management
  flutter_riverpod: ^2.4.9
  
  # Database (placeholder - implement based on your stack)
  # sqflite: ^2.3.0
  # drift: ^2.14.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
